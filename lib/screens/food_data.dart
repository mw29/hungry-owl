import 'package:flutter/material.dart';

class FoodData extends StatelessWidget {
  const FoodData({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
  
      body: const Center(
        child: Text('Food data will be displayed here.'),
      ),
    );
  }
}
